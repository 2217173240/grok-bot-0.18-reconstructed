# 本地运行维护计划

更新日期：2026-09-24。代码复核基线：`f87850a`。架构与实际验收范围见 [LOCAL-SANDBOX-ARCHITECTURE.md](LOCAL-SANDBOX-ARCHITECTURE.md)。

## 已交付能力

- local admin、容器内 agent 回合、第三方推理、文件与 MCP、Computer、图片附件及前台 Task 结果交付。
- Colima `grokbot` 为默认环境，OrbStack 为可选 Docker socket 来源；本地回合没有 Mac 执行选项。
- 基础镜像由独立 `grok-bot-box-image` 仓库提供固定 Release 归档，下载与导入均校验身份；原版安装包统一登记官方来源与 SHA-256。
- 发布包保留固定原版 renderer，源码运行模块独立编译，构件组成、原生依赖、ASAR 与签名分别验证。
- 镜像与浏览器 profile 持久化、会话同步、随机 noVNC 凭证、窗口配额、桌面健康探测与明确重建已有真实容器验证。

## 当前处理顺序

| 顺序 | 事项 | 需要满足的行为 | 状态 |
| --- | --- | --- | --- |
| 1 | build-stamp / 依赖 pin 读取 | 区分开发构建无文件与发布包文件损坏；非法 pin、读取错误不得关闭镜像校验；并发读取共享完成结果 | 待修复 |
| 2 | PR #63：Claude 凭据挂载与 OrbStack 构建 | provider 配置损坏明确报错；只有明确选择 Claude 才考虑凭据挂载；保留作者的 OrbStack 支持 | 待修复并验证 |
| 3 | PR #64：失败消息补发状态 | 状态损坏不得返回空集合；失败 ID 不因固定条数上限重新获得补发资格；验证取消与新消息恢复 | 待修复并验证 |
| 4 | PR #65：缓存用量 | 使用当前 SDK 的缓存字段分开统计，覆盖缺失、零值与上限；不扩大真实账号验收范围 | 已审查，待处理 |
| 5 | Codex 配置 | 配置缺失与不可读、非法格式分别处理；模型与 reasoning 使用同一解析规则 | 待修复 |
| 6 | ActionAudit outbox | 读取与写入失败有明确结果；本地 JSONL 写入失败可见；关闭后端转发时不持续积累无消费队列；容量策略明确报告 | 待修复 |
| 7 | Episode 记忆维护 | interval 与容量兼容；总结失败保留尚未处理的批次；不自动重放产生外部副作用的工具 | 待修复 |
| 8 | 结构化日志 checkpoint | 检查恢复时超过容量的记录是否计入丢弃统计；复用现有 overflow 处理 | 待验证并修复实际缺口 |
| 9 | 本地 host 生命周期测试 | 使用测试持有的端口与真实进程；不因生产服务占用 1340 而跳过 | 待修复 |

每项完成时更新本表，并记录实际验证范围。源码和 CI 通过后，影响安装包与运行路径的改动继续完成构建及相应实际验收。

## 保留的容量与版本边界

- 请求 ID 持久历史保留最近 200 条。当前错误关联使用最新 ID，当前回合另有内存记录；未发现该上限影响执行正确性。
- memory synthesis 的待处理证据是后台内存队列，超限已有 `dropped` 报告；完整对话保存在 transcript。它与决定消息补发的权威状态分别管理。
- 结构化日志运行期超限已有 `overflow_evicted` 计数与 `dropped` 通知；只核对 checkpoint 恢复的实际缺口。
- 当前支持 macOS arm64、固定 Electron 42 与 AI SDK v4。升级时验证 safeStorage、SDK 工具/流/用量、原生模块和 CLI 入口；不在本轮引入跨平台重写或大版本迁移。
- 开发构建使用 Node 26.5.0；容器 Node 22.23.2 的兼容范围由实际容器门禁验证。版本差异与 experimental 警告单独记录，不能直接当作运行失败。
- 模型默认值可覆盖；配置可读性错误必须在请求 API 前报告。模型可用性仍由具体 provider 与账号决定。

## 保留的验收条件

- OpenRouter、Command Code 有效账号回合仍需用户提供已配置账号；Command Code 的真实无效密钥路径已验证。
- Codex 仅在隔离容器只读使用现有凭据；本轮不将账号目录挂入生产容器。
- passkey、真人登录与交回需用户提供测试站点并参与；自动化路径的通过不覆盖这些操作。
- 公开分发原版资源继续遵循 PROVENANCE.md；当前复用固定构件的目标保持不变。
- 桌面模式的容器隔离、浏览器自身沙箱、workspace bind 与出口控制分别审查。应用层网络 guard 无法替代出口防火墙。
- 历史改动与原始验证记录保存在 Git 历史及对应专题文档；当前工作状态以本页和架构文档为准。
